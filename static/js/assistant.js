document.addEventListener('DOMContentLoaded', () => { const card=document.querySelector('.ask-card'); if(!card)return; const input=document.getElementById('plant-question'), button=document.getElementById('ask-button'), output=document.getElementById('plant-answer'); async function ask(){const question=input.value.trim(); if(!question){output.hidden=false;output.textContent='Please type a question first.';return;} button.disabled=true;button.textContent='Finding guidance…'; try{const r=await fetch(card.dataset.askUrl,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question})});const d=await r.json();output.textContent=d.answer||d.error||'No answer available.';output.hidden=false;}catch{output.textContent='The helper is temporarily unavailable. Try again.';output.hidden=false;}finally{button.disabled=false;button.textContent='Ask →';}} button.addEventListener('click',ask); input.addEventListener('keydown',e=>{if(e.key==='Enter')ask();});});
